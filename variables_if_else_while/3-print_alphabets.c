#include <stdio.h>

/**
 * main - charput and char to recreate alpha in lower followed by upper
 * Return: 0 to sucess
 */
int main(void)
{
	char alphabet = 'a';
		while (alphabet <= 'z')
	{
		putchar(alphabet);
		alphabet++;
	}
{
	char ALPHABET = 'A';
		while (ALPHABET <= 'Z')
		{
		putchar(ALPHABET);
		ALPHABET++;
		}
	putchar('\n');
	return (0);
}}
